# Assignment for extraction@1

From the selected supported discovery records, prepare at most one instruction implementation worth testing. Its policy must specify entry observations, conditional decisions, required tools and permissions, exit obligations, and abstention conditions.

Use instruction_contract and exact source/version linkage. Declare bindings instead of embedding donor paths, outputs or answers. Preserve counterexamples and unresolved assumptions. Package a candidate only: discovery and extraction do not grant production admission.

Do not substitute a registered prewritten solver selected by the motif name. The recipient will execute these instructions through the ordinary Pi prepared-invocation path.
