# Assignment for extraction@1

From the selected supported discovery records, prepare at most one instruction implementation worth testing. Its policy must specify entry observations, conditional decisions, required tools and permissions, exit obligations, and abstention conditions.

Use instruction_contract and exact source/version linkage. Declare bindings instead of embedding donor paths, outputs or answers. Preserve counterexamples and unresolved assumptions. Package a candidate only: discovery and extraction do not grant production admission.

Do not substitute a registered prewritten solver selected by the motif name. The recipient will execute these instructions through the ordinary Pi prepared-invocation path.

State what the recipient learns to do beyond the task description. Preserve the operative procedure: which observations to obtain, which distinctions to make, what action each distinction supports, and how to check the result. Applicability and abstention guidance accompany that procedure. If the source leaves an essential step unresolved, retain that gap and explain what evidence is needed before testing it.

Check the instruction against the supplied recipient interface. Walk through one supported path and one nearby case requiring a different decision, using source evidence without copying donor answers. Save the useful procedure with its limits; label whether it came from enacted agent decisions or a procedure described in inspected material.
